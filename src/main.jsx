import React from 'react'
import ReactDOM from 'react-dom/client'
import ChumleaCalculator from './ChumleaCalculator'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ChumleaCalculator />
  </React.StrictMode>,
)